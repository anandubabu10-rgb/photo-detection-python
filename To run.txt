
Perfect 👍
I’ll give you a **clean, repeatable setup guide** you can use on any new Windows system.

No trial-and-error. No guessing.

---

# 🚀 FULL SETUP GUIDE (Windows, Python 3.10, Face Embedding API)

---

# ✅ STEP 1 — Install Python (Correct Version)

🔹 Download Python **3.10.x (64-bit)**
From:

👉 [https://www.python.org/downloads/release/python-31011/](https://www.python.org/downloads/release/python-31011/)

⚠ Important during installation:

✔ Check **“Add Python to PATH”**
✔ Click **Customize Installation → Install for all users**

After install, open CMD:

```bash
python --version
```

You MUST see:

```text
Python 3.10.x
```

---

# ✅ STEP 2 — Create Project Folder

```bash
mkdir faceService
cd faceService
```

---

# ✅ STEP 3 — Create Virtual Environment

```bash
python -m venv venv
```

Activate it:

```bash
venv\Scripts\activate
```

You should now see:

```text
(venv)
```

---

# ✅ STEP 4 — Upgrade pip

```bash
python -m pip install --upgrade pip
```

---

# ✅ STEP 5 — Install Required Packages (Correct Order)

⚠ VERY IMPORTANT ORDER

---

## 5.1 Install Compatible NumPy (NOT 2.x)

```bash
pip install numpy==1.26.4
```

---

## 5.2 Install Precompiled dlib (NO BUILD ERROR)

Download this file:

👉 [https://www.lfd.uci.edu/~gohlke/pythonlibs/#dlib](https://www.lfd.uci.edu/~gohlke/pythonlibs/#dlib)

Download:

```
dlib_bin-19.24.2-cp310-cp310-win_amd64.whl
```

Then install it:

```bash
pip install C:\Path\To\Downloads\dlib_bin-19.24.2-cp310-cp310-win_amd64.whl
```

(Replace path with your actual download location)

Install requirements.txt also
pip install -r requirements.txt --no-deps

---

## 5.3 Install Remaining Dependencies

```bash
pip install fastapi uvicorn opencv-python-headless Pillow python-multipart
```

---

## 5.4 Install face_recognition WITHOUT rebuilding dlib

```bash
pip install face_recognition --no-deps
```

---

# ✅ STEP 6 — Create `main.py`

Create a file called:

```
main.py
```

Paste this:

```python
from fastapi import FastAPI, UploadFile, File, HTTPException
import face_recognition
import numpy as np
import cv2

app = FastAPI(title="Face Embedding Service")


@app.post("/generate-embedding")
async def generate_embedding(file: UploadFile = File(...)):
    contents = await file.read()

    np_arr = np.frombuffer(contents, np.uint8)
    img = cv2.imdecode(np_arr, cv2.IMREAD_COLOR)

    if img is None:
        raise HTTPException(status_code=400, detail="Invalid image")

    rgb_img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)

    encodings = face_recognition.face_encodings(rgb_img)

    if len(encodings) == 0:
        raise HTTPException(status_code=404, detail="No face detected")

    embedding = encodings[0].tolist()

    return {
        "embedding": embedding,
        "dimensions": len(embedding)
    }


@app.get("/health")
def health():
    return {"status": "running"}
```

---

# ✅ STEP 7 — Run Server

```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

You should see:

```
Uvicorn running on http://0.0.0.0:8000
```

---

# ✅ STEP 8 — Test API

Open browser:

```
http://localhost:8000/docs
```

Test:

* POST /generate-embedding
* Upload JPG or PNG
* Must contain clear face

Expected output:

```json
{
  "embedding": [...],
  "dimensions": 128
}
```

---

# 🧠 What You Just Avoided

This setup avoids:

❌ CMake error
❌ Microsoft C++ build tools
❌ dlib compilation
❌ numpy 2.x incompatibility
❌ Pillow missing error

---

# 🎯 If You Want Even Cleaner Setup (Professional Way)

For production or scaling:

* Use Ubuntu server
* Or Docker container
* Avoid Windows ML headaches

If you want, I can give you:

* 🐳 Dockerfile version
* 🐧 Ubuntu version
* 📦 requirements.txt production version

---

You now have a clean repeatable installation plan.

If you want, I can also give you a single `.bat` file that sets everything up automatically.
